import React from "react";
import UserRegister from "../../components/Auth/UserAuth/Register/UserRegister";

type Props = {};

const UserRegisterPage = (props: Props) => {
  return <UserRegister />;
};

export default UserRegisterPage;
